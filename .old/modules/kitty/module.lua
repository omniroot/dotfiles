local packages = {

}

return {
    description = "Kitty",
    -- conflicts = { "bdots-kde" },
    -- post_install_hook = "scripts/install-hyprland-dotfiles.sh",
    -- hook_behavior = "once",
    dotfiles_sync = false,
    packages = packages,
}
